import { Canvas, Meta } from '@storybook/blocks';

import * as BannerCodeWordStories from './securitybanner.codewords.stories';

<Meta name='Codeword Examples' of={BannerCodeWordStories} />

# Create a Basic Security Banner

The React Classification Banner aims to provide a UK Security Classification banner which will default to the most basic security classification banner. In this example we are showcasing the default behavour it the table.
# Demonstration

<Canvas of={BannerCodeWordStories.OFFICIAL_CODEWORDS} />
<Canvas of={BannerCodeWordStories.SECRET_CODEWORDS} />
<Canvas of={BannerCodeWordStories.TOP_SECRET_CODEWORDS} />

# Source code

```ts
const BasicExampleComponent: FC<SecurityBannerProps[]> = ({records}) => {

  // Generates a default instance of the banner
  return (<SecurityBanner  records: {records} />);
};
```
